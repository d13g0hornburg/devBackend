aleatorioMinMax();

function aleatorioMinMax(min, max) {
    min = 10;
    max = 20;
    resultado = Math.random() * (max - min) + min;
    console.log("Número aleatório entre o max e min é: " + resultado.toFixed(0));
  }

  
  